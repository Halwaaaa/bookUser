import 'package:flutter/material.dart';

class Advenced extends StatelessWidget {
  const Advenced({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Advenced"),
      ),
    );
  }
}
